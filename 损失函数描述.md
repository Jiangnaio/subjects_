## 1. Multi-Positive InfoNCE Loss（`info_nce`）

这是对标准 **InfoNCE（Noise Contrastive Estimation）** 损失的推广，允许每个查询（query）对应多个正样本（positive topics）。目前表现最优的损失函数，支持梯度累积，在Arctic-Embed, Qwen3-Embedding, splade等模型上均有较好的提升。

### 数学符号说明：

- $ B $：batch size（查询数量）
- $ K $：每个查询对应的候选主题数量（包括正样本、负样本、硬负样本）
- $ \mathbf{q}_i \in \mathbb{R}^d $：第 $ i $ 个查询的嵌入向量（已归一化）
- $ \mathbf{t}_{i,j} \in \mathbb{R}^d $：第 $ i $ 个查询对应的第 $ j $ 个候选主题的嵌入向量（已归一化）
- $ y_{i,j} \in \{0, 1\} $：标签，1 表示正样本，0 表示负样本
- $ \tau > 0 $：温度参数（temperature）

### 相似度计算：

余弦相似度（因嵌入已归一化，等价于点积）：
$
s_{i,j} = \frac{\mathbf{q}_i^\top \mathbf{t}_{i,j}}{\tau}
$

### 损失函数（每查询）：

对每个查询 $ i $，其损失为：
$
\mathcal{L}_i = \log \left( \sum_{j=1}^{K} e^{s_{i,j}} \right) - \log \left( \sum_{j: y_{i,j}=1} e^{s_{i,j}} \right)
$

等价于：
$
\mathcal{L}_i = \log \left( \frac{\sum_{j=1}^{K} e^{s_{i,j}}}{\sum_{j: y_{i,j}=1} e^{s_{i,j}}} \right)
$

### 总损失（平均）：

$
\mathcal{L}_{\text{InfoNCE}} = \frac{1}{|\mathcal{B}_{\text{valid}}|} \sum_{i \in \mathcal{B}_{\text{valid}}} \mathcal{L}_i
$
其中 $\mathcal{B}_{\text{valid}} = \{ i \mid \exists j, y_{i,j} = 1 \}$ 是至少包含一个正样本的查询索引集合。

> **注**：这是标准 InfoNCE 的多正样本扩展，**仅在同一个查询的候选集内进行归一化（in-batch negatives 被忽略）**，也称为 **in-batch InfoNCE with local negatives**。

---

## 2. Supervised Contrastive Loss（`supcon` 风格）

这是对 **Supervised Contrastive Learning (SupCon)** 的一种变体，但以 **查询为锚点（anchor）**，将整个 batch 中的所有候选主题（共 $ B \times K $ 个）作为对比池。该函数的训练效果与batch_size高度相关，且梯度累积无效。

### 符号扩展：

- 所有候选主题展平为集合：$\{ \mathbf{t}_m \}_{m=1}^{B K}$
- 对应的“全局”标签：对于查询 $ i $，只有其自身的正样本（即 $ j $ 满足 $ y_{i,j}=1 $）被视为正，其他所有（包括其他查询的正/负样本）均为负。

### 相似度矩阵：

$
S_{i,m} = \frac{\mathbf{q}_i^\top \mathbf{t}_m}{\tau}, \quad m = 1, 2, \dots, B K
$

### 正样本指示（全局）：

定义全局正样本掩码：
$
\mathcal{P}_i = \{ m \mid \text{topic } m \text{ belongs to query } i \text{ and } y_{i, j(m)} = 1 \}
$

### 损失函数（每查询）：

$
\mathcal{L}_i = \log \left( \sum_{m=1}^{B K} e^{S_{i,m}} \right) - \log \left( \sum_{m \in \mathcal{P}_i} e^{S_{i,m}} \right)
$

### 总损失：

$
\mathcal{L}_{\text{SupCon}} = \frac{1}{|\mathcal{B}_{\text{valid}}|} \sum_{i \in \mathcal{B}_{\text{valid}}} \mathcal{L}_i
$

> **关键区别**：与 InfoNCE 不同，SupCon 风格损失使用 **整个 batch 的所有候选** 作为负样本（包括其他查询的正样本），因此对比范围更大，理论上能提供更强的判别能力，但也可能引入“假负样本”（false negatives）——即其他查询的正样本其实语义上与当前查询相关。

---

### 总结对比

| 特性             | `info_nce`       | `supcon`                                 |
| -------------- | ---------------- | ---------------------------------------- |
| 负样本范围          | 仅当前查询的 $ K $ 个候选 | 整个 batch 的 $ B \times K $ 个候选            |
| 正样本范围          | 仅当前查询的正样本        | 仅当前查询的正样本                                |
| 是否跨查询对比        | ❌ 否              | ✅ 是                                      |
| 计算复杂度          | $ O(BK) $        | $ O(B^2 K) $                             |
| 假负风险           | 低                | 高（若不同查询的正样本语义重叠）                         |
| 与标准 InfoNCE 关系 | 多正样本扩展           | 类似 SupCon，但锚点是 query，正样本是其 own positives |

---

### 附：标准 InfoNCE 与 SupCon 回顾

- **标准 InfoNCE**（SimCLR 风格）：
  $
  \mathcal{L}_i = -\log \frac{e^{s_{i,i^+}/\tau}}{\sum_{j \ne i} e^{s_{i,j}/\tau}}
  $
  通常用于成对数据（如增强视图）。

- **SupCon**（Khosla et al., 2020）：
  $
  \mathcal{L}_i = \frac{1}{|P(i)|} \sum_{p \in P(i)} \left[ -\log \frac{e^{s_{i,p}/\tau}}{\sum_{a \in A(i)} e^{s_{i,a}/\tau}} \right]
  $
  其中 $ P(i) $ 是所有与 $ i $ 同类的样本，$ A(i) $ 是所有其他样本（包括同类和异类）。

代码中的 `supcon` 实现是 **query-anchor + global candidates** 的定制版本，非标准 SupCon，但思想类似。

--- 



该代码中使用的损失函数是 **带正则化的改进对比损失（Improved Contrastive Loss with Regularization）**，其完整形式如下：

---

## 3.多主题对比损失函数

### 🧮 损失函数定义

对于每个查询（query）$ q $，设其对应的正样本集合为 $\mathcal{P}_q $负样本集合为$mathcal{N}_q $括普通负样本和硬负样本），总相关文本嵌入为$ \{ t_i \}_{i \in \mathcal{P}_q \cup \mathcal{N}_q} ，$的标签为：
$
y_i = 
\begin{cases}
1, & i \in \mathcal{P}_q \\
0, & i \in \mathcal{N}_q
\end{cases}
$

令：

- $ \mathbf{e}_q \in \mathbb{R}^d $：查询$q$的归一化嵌入；
- $ \mathbf{e}_i \in \mathbb{R}^d $：文本$i$的归一化嵌入；
- 余弦相似度：$ s_{q,i} = \mathbf{e}_q^\top \mathbf{e}_i \in [-1, 1] $；
- 余弦距离：$ d_{q,i} = 1 - s_{q,i} \in [0, 2] $；
- 温度参数：$ \tau = \texttt{CONFIG["temperature"]} $（默认 0.07）。

---

### 🔹 1. 基础对比损失（每 query）

对每个 query $ q $，其损失由三部分组成：

#### (a) 正样本损失（鼓励靠近）

$
\mathcal{L}_{\text{pos}}^{(q)} = \frac{1}{|\mathcal{P}_q|} \sum_{i \in \mathcal{P}_q} d_{q,i}^2
$

> 即最小化正样本的**余弦距离平方**。

#### (b) 负样本损失（带边界 hinge）

$
\mathcal{L}_{\text{neg}}^{(q)} = \frac{1}{|\mathcal{N}_q|} \sum_{j \in \mathcal{N}_q} \max\left(0,\, \alpha - d_{q,j} \right)
$

> 其中 $ \alpha \in [\alpha_{\min}, \alpha_{\max}] $ 是**自适应边界**，随训练步数指数衰减：
> $
> \alpha(t) = \alpha_{\min} + (\alpha_{\max} - \alpha_{\min}) \cdot \gamma^t,\quad \gamma = \texttt{alpha\_decay} < 1
> $

#### (c) 多样性正则项（正样本间差异）

若 $ |\mathcal{P}_q| > 1 $，计算正样本嵌入两两之间的余弦相似度矩阵$S_{\text{pos}} \in \mathbb{R}^{|\mathcal{P}_q| \times |\mathcal{P}_q|}$其中：
$
[S_{\text{pos}}]_{k,l} = \mathbf{e}_{p_k}^\top \mathbf{e}_{p_l}
$
排除对角线后，鼓励相似度接近目标值（如 0.5）：
$\mathcal{R}_{\text{div}}^{(q)} = \frac{1}{|\mathcal{P}_q|(|\mathcal{P}_q|-1)} \sum_{k \ne l} \left( [S_{\text{pos}}]_{k,l} - 0.5 \right)^2
$

#### (d) 熵正则项（鼓励预测分布均匀）

对所有相关样本（正+负）计算 softmax 分布：
$
p_i^{(q)} = \frac{\exp(s_{q,i} / \tau)}{\sum_{k \in \mathcal{P}_q \cup \mathcal{N}_q} \exp(s_{q,k} / \tau)}
$
然后计算熵：
$H^{(q)} = - \sum_{i} p_i^{(q)} \log(p_i^{(q)} + \epsilon),\quad \epsilon = 10^{-8}
$

> **注意**：在最终损失中，**熵项被减去**，即鼓励熵增大（分布更均匀）。

---

### 🔸 2. 单 query 总损失

$
\mathcal{L}^{(q)} = \mathcal{L}_{\text{pos}}^{(q)} + \lambda_{\text{w}} \cdot \mathcal{L}_{\text{neg}}^{(q)} + \lambda_{\text{reg}} \cdot \mathcal{R}_{\text{div}}^{(q)} - \lambda_{\text{ent}} \cdot H^{(q)}
$

其中超参数来自 CONFIG：

- $ \lambda_{\text{w}} = \texttt{lambda\_weight} $（默认 0.8）
- $ \lambda_{\text{reg}} = \texttt{diversity\_weight} $（默认 0.15）
- $ \lambda_{\text{ent}} = \texttt{lambda\_entropy} $（默认 0.03）

---

### 🔸 3. 批次总损失（平均）

设批次中有 $ Q $ 个有效 query（即同时含正负样本），则最终损失为：
$
\boxed{
\mathcal{L} = \frac{1}{Q} \sum_{q=1}^{Q} \mathcal{L}^{(q)}
}
$

若某 query 无正样本或无负样本，则跳过（不参与损失计算）。

---

### ✅ 总结：损失函数目标

| 项                            | 目标                             |
| ---------------------------- | ------------------------------ |
| $ \mathcal{L}_{\text{pos}} $ | 拉近 query 与正样本                  |
| $ \mathcal{L}_{\text{neg}} $ | 推远 query 与负样本（至少距离 $ \alpha $） |
| $ \mathcal{R}_{\text{div}} $ | 防止多个正样本 collapse 到同一点          |
| $ -H $                       | 防止模型对某个样本过度自信，提升泛化             |

该设计特别适合**多正样本检索任务**（如学术主题分类），兼顾精度、多样性和鲁棒性。
